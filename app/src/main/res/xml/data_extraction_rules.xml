<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Include or exclude files for cloud backup -->
        <!-- Example: <exclude domain="file" path="secret_folder/"/> -->
    </cloud-backup>
    <device-transfer>
        <!-- Include or exclude files for device transfer -->
        <!-- Example: <exclude domain="file" path="secret_folder/"/> -->
    </device-transfer>
</data-extraction-rules>
